<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{ url_for('static', filename='css/login.css') }}">
    <title>Login Page</title>
</head>
<body>
    <div class="main">
    <p class="sign" align="center">Войти</p>
        {% from "_formhelpers.html.jinja" import render_field %}
        <form class="loginForm" method="post">     
            {{ render_field(form.login, class="login", align="center", placeholder="Логин") }}
            {{ render_field(form.password, class="password", align="center", placeholder="Пароль") }}
            <input type="submit" value="Login" class="submit">
        </form>
        {% with messages = get_flashed_messages() %}
            {% if messages %}
                <ul class=flashes>
                    {% for message in messages %}
                        <li>{{ message }}</li>
                    {% endfor %}
                </ul>
            {% endif %}
        {% endwith %}
    </div>
</body>
</html>